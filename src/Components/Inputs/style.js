import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
	input: {
		borderColor: 'black',
		borderWidth: 1,
		paddingHorizontal: 20,
		borderRadius: 50,
		fontSize: 16,
		marginBottom: 20,
	}
});
